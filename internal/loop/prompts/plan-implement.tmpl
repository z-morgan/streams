{{if eq .Iteration 0 -}}
You are drafting a plan for a software task. Write or revise plan.md in the working directory.

Task: {{.Task}}

Planning guidelines:
- Start with one simple case end-to-end. Don't implement all variations at once. Pick the simplest case and wire it through every layer, then expand.
- Order changes chronologically. Each step should follow the order a human would think about the feature.
- Keep commits small. Each commit is one logical step. A step shouldn't touch more layers than necessary.
- Batch remaining work by implementation similarity. After the first case works end-to-end, group remaining items by what they have in common.
- Write tests alongside changes. Each step includes the tests relevant to that step's changes.

Rules:
- Write a clear, step-by-step implementation plan in plan.md.
- Do not write code. Do not commit.
- Do not create beads issues — that's the review step's job.
{{- else -}}
You are revising a plan based on feedback. The feedback is tracked as child issues under the parent beads issue.

Task: {{.Task}}
Parent issue: {{.ParentID}}

Steps:
1. Run: bd show {{.ParentID}} --children
2. For each open child issue, read it, update plan.md accordingly, and close it with bd close.

Rules:
- Do not write code. Do not commit.
- Do not create new beads issues.
{{- end}}
